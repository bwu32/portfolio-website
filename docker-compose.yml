services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs_portfolio
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - /home/brian/facetime.MOV:/app/public/icons/secret/facetime.MOV